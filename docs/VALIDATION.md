# 测试与评测

当前结论在前，历史失败与复测记录保留在末尾折叠区。执行器验证、模型对照、产品浏览器回归是三类不同证据，不混用分母。

## 最近验证结果

截至 2026-09-23 的本地记录：
- `npm run check` 通过：56 条单元测试、API TypeScript 构建和 Web 构建。
- `REPROLENS_BROWSER_CHANNEL=chrome npm run demo:check` 使用本机 Chrome 和隔离的演示服务，分别验证购物车数量、手机弹窗遮挡、输入失焦丢值三条真实浏览器流程。每条均在缺陷版判定为 `reproduced`，在修复版判定为 `not_reproduced`，并检查核心业务验证状态、步骤与截图。
- 上述固定演示不调用模型（模型调用次数为 0），不能作为模型规划能力或真实客户网站成功率的证据。浏览器未就绪时，API 会在创建运行前返回 `BROWSER_UNAVAILABLE`；界面显示排查提示并禁用执行。
- 用模拟浏览器缺失响应验证了工作台的禁用状态和错误提示；1440×1000、390×844 视口均检查了布局。此项仅验证失败反馈，不等同于真实浏览器缺失环境的端到端测试。

截至 2026-09-15 的本地记录：
- 54 条单元测试通过，前后端 TypeScript 与 Vite 构建通过。
- 11 个真实本机浏览器任务，覆盖 UI/输入双版本、质量开关、运行错误、仍复现、受阻、标准变更及页面加载失败。
- 同一导出文件通过完整 Playwright runner：缺陷版桌面通过、手机在遮挡断言失败；修复版均通过。
- 1440×1000 / 390×844 页面无横向溢出或脚本异常。
- 产物：`artifacts/reproduction-qa-1789484942090/`。这不是 11 个真实客户网站，也不是模型成功率；本轮未发布真实 GitHub Check。

## 快速运行

在仓库根目录安装依赖与浏览器后运行：

```sh
npm run check
npm run demo:check
npm run eval:smoke
# 按需执行全部固定样本
npm run eval:full
```

执行器 CLI 自带隔离演示服务，不要求启动工作台。成功返回 0；样本不符、浏览器缺失或进程异常返回非零。浏览器下载受限时可用已安装的 Chrome：给 API/CLI 设置 `REPROLENS_EVAL_CHANNEL=chrome`，使用独立上下文而非个人浏览器资料。

`demo:check` 与评测 CLI 是不同入口：它复用产品的运行管理器，对三种内置故障执行缺陷/修复双版本闭环；使用系统 Chrome 时设置 `REPROLENS_BROWSER_CHANNEL=chrome`。报告写入临时目录，终端会打印路径。

产品专项验证先构建并启动 API，再执行：
```sh
REPROLENS_TEST_URL=http://127.0.0.1:8787 REPROLENS_BROWSER_CHANNEL=chrome node scripts/verify-reproduction.mjs
```

系统 Chrome 必须已安装；默认 Playwright Chromium 环境可不指定 channel。API 与脚本使用一致浏览器配置。评测 UI 专项为 `node scripts/verify-evaluation.mjs`。

## 固定计划执行器评测

复用 `apps/api/src/business.ts`，不是另一套模拟执行器，也不调用模型。每条使用新 BrowserContext；计划经 Zod 校验，实际判定和步骤状态与标注比较，再生成同一测试文件在双版本重跑。

数据集位于 `apps/api/src/evaluation/cases.ts`，页面在 `fixtures.ts`。10 类场景、每类两版，共 20 条：失焦丢值、禁用、只读、成功提示缺失、文案错误、遮罩未消失、延迟提示、同名控件、未授权危险动作、前置标识缺失。添加用例必须同步真实页面行为，不能只加预设结果。

冒烟为 `blur-bug`、`blur-fixed`、`ambiguous-bug`、`danger-bug`。危险动作需检查未发生点击，不能仅凭“证据不足”文字宣称安全。

| 指标 | 分母与含义 |
| --- | --- |
| 用例符合率 | 判定、步骤和额外安全检查符合 / 已执行条数 |
| 判定准确率 | 实际判定等于标注 / 已执行条数，包括运行错误 |
| 误报率 | 正常样本被判已复现 / 正常样本数 |
| 证据不足率 | 证据不足 / 已执行条数 |
| 平均耗时 | 用例执行耗时，不含生成测试重跑 |

分母为零返回 null。安全拦截可算符合预期，但不是成功复现。固定计划的模型调用为 0，token 和费用为 null。

### 双版本与产物

`artifacts/eval-<uuid>/` 保留 report.json、manifest.json、regression.spec.ts、Playwright 配置、replay-bug.json / replay-fixed.json 和步骤截图。manifest 记录环境、计划及生成代码哈希。

失焦样本的缺陷版必须在指定失焦断言行失败，期望 repro-test、实际为空；输入前失败、导航异常、无结果或超时不算检出。修复版必须通过且正常退出。全部选定样本和两次重跑完成才算任务通过。

隔离服务关闭后，手动重跑需启动 API，设置 `REPROLENS_TARGET_URL` 指向 `/demo/eval/blur?fixed=0` 或 `?fixed=1`，使用产物配置运行，不能改断言。

### 评测界面与 API

展开“开发者工具”进入评测实验室。API 为 GET /api/evaluations/cases、POST /api/evaluations（suite 为 smoke/full）、GET /api/evaluations/latest。重复运行返回 409，非法 suite 返回 422。

界面轮询最新状态；最新任务索引在进程内，重启后不恢复，CLI 与 API 索引不共享；磁盘完整产物仍保留。不同浏览器版本的成绩不应直接比较。

<a id="model-evaluation"></a>

## 模型观察对照

单独验证“初始页面结构是否改善计划”，不改变用户期望，不实现自主重规划。观察配置见 [使用说明](USAGE.md#页面观察)。

```sh
npm run eval:model
# 每组每场景重复三次，共 18 次调用
npm run eval:model -- 3
```

需要有效 DeepSeek Key，会产生 API 调用费用。可设置 `REPROLENS_BROWSER_CHANNEL=chrome`。默认三个合成场景（失焦、禁用、只读），无观察/有观察各一次，共六次调用。

两组同模型、temperature=0、同 Prompt 与 Schema，只有 observation 字段不同；交替顺序仅缓解偏差。每份计划在缺陷版规划一次，原样执行两版。不向模型发送源码、缺陷开关或人工答案。

双版本正确须同时满足：模型成功返回合法计划而非模板；真正检查目标输入能力；缺陷版核心失败，修复版所有步骤通过。错误定位、执行异常和未完成均计失败。对照最多六步，仅 input/assert；这不是主产品的全部动作范围。

产物为 `artifacts/model-compare-<id>/report.json` 和 summary.md，保留数据集版本、观察、计划哈希、原始合成输出、真实 token、步骤与失败。未返回用量为 null。

### 实际结果与限制

第一轮 observe-plan-1 两组均 0/3，暴露必填授权字段遗漏和步骤契约理解错误。随后澄清 Prompt，未放宽 Schema 或判分；另有一次诊断调用未计入对照。

第二轮统一 observe-plan-2：
| 模式 | 双版本正确 | 平均端到端耗时 |
| --- | --- | --- |
| 无观察 | 0/3 | 6.35 秒 |
| 有观察 | 3/3 | 13.12 秒 |

第一轮目录：`artifacts/model-compare-66cfc5a8-e712-48e6-8d2d-69ca3e8c3236/`。
第二轮目录：`artifacts/model-compare-bfd7177e-ee1c-4133-a737-b69107d5f9ac/`。

样本参与调试，不是留出集；不能写“准确率 100%”，不能证明真实网站或 UI 场景泛化。耗时含观察、模型与双版本执行，失败组可能提前结束，不是纯模型延迟比较。

观察专项为 `REPROLENS_BROWSER_CHANNEL=chrome node scripts/verify-observation.mjs`；验证工作台布局时先启动服务，并用 REPROLENS_TEST_URL 指定本机端口。

## 历史验证记录

以下保留原始失败、复测、任务 ID 和产物路径。历史版本号只标识当时快照；旧“未覆盖”项、界面描述及成绩不覆盖上方最新结论。目录中的产物默认不提交 Git，复查需保留本机数据。

<details>
<summary>展开历史测试与复测记录（2026-09-09 至 2026-09-15）</summary>

# ReproLens 测试报告汇总

本文档按版本汇集测试范围、执行结果、已知问题与复测记录。后续版本在此追加独立章节，不再创建带版本号的测试报告文件；同一版本的复测按日期追加，保留历史结论。

## 展示方式与布局复核（2026-09-10）

- 移除线上静态展示模式、预置任务、示意图与 Vercel 配置，保留本地真实执行和内置故障商城。
- `npm run build` 通过；`node scripts/verify-layout.mjs` 验证问题列表直接展示、1440 / 1920 宽度下两组卡片等高及底部间距、390 宽度无页面横向溢出。
- `node scripts/verify-v05-ui.mjs` 的模拟 API 界面冒烟通过，覆盖默认空表单、计划确认和重新编辑。
- `node scripts/record-demo.mjs` 使用真实 API / Chromium、独立临时存储及本机注册表单：只读输入框判定 `reproduced`；修复后重放同一计划判定 `not_reproduced`，核心输入检查通过。未调用模型、未使用真实账号，未执行生成的测试文件；视频约 41 秒，现已转为 `docs/media/reprolens-demo.mp4`（H.264，保留原分辨率，完整解码检查通过）。
- 本轮是针对性验证，不替代全部浏览器场景或线上部署验收。历史报告保留如下。

## 版本索引

| 版本 | 测试日期 | 结论 |
| --- | --- | --- |
| [v0.5](#v05) | 2026-09-09 | 三项已知问题已修复；23 项单元测试、18 个浏览器场景与 UI 冒烟通过 |

<a id="v05"></a>

## v0.5：业务断言驱动的 Bug 复现

### 修复后复测（2026-09-09）

本轮修复并关闭首次测试记录中的三项问题：

1. 将动作后的辅助状态读取改为容错采集。控件因切换页面或 DOM 更新消失时，记录当前页面或采集不可用提示，不改变已成功的动作结论；下一步场景断言正常执行。
2. visible / hidden 断言不再被通用“必须先找到且可见”检查截断。核心检查会等待目标出现；超时未出现或仍隐藏则判为失败。前置检查失败仍为受阻，多匹配仍为歧义，不误报已复现。导出测试同步调整定位前置逻辑。
3. 更新旧 GitHub 报告的草稿预期，并补充 generated / draft 文案和核心检查覆盖率对应的 Check 结论测试。

| 复测范围 | 结果 |
| --- | --- |
| npm run check | 23 / 23 单元测试通过；API/Web 构建通过 |
| 原有本地 Chromium 场景 | 15 / 15 通过 |
| 新增边界场景 | 3 / 3 通过：提示隐藏、延迟出现、重复匹配 |
| 导出测试体 | 与对应场景的故障/正常预期一致，注册场景切换差异已消除 |
| UI 冒烟 | 默认空值、计划预览、确认门禁、请求修改失效、提交与重编辑通过 |

浏览器验证共覆盖 18 个独立场景：原有 15 项整组执行，新增 3 项分别执行。已加入同一脚本，后续可用 `node scripts/verify-v05.mjs` 一次重跑全部。未调用付费模型或操作真实账号；测试产生的临时截图及进程已清理。

结论：本次指定的三项缺陷已修复并通过回归。下面保留首次测试的原始结果，不代表当前缺陷状态。真实模型、完整 API/SSE、全设备矩阵与 CI runner 的未覆盖范围仍然有效，不据此宣称完整产品验收通过。

### 首次测试记录（修复前，2026-09-09）

日期：2026-09-09。结论：核心输入验证可用，但尚不建议判定 v0.5 全部验收通过；发现两处行为缺陷和一处旧测试预期失效。本轮仅添加验证脚本与报告，未修改生产逻辑或用户已暂存的改动。

### 执行结果

| 验证层 | 结果 |
| --- | --- |
| 原有 Vitest 测试 | 20 / 21 通过；GitHub 报告旧断言失败 |
| API TypeScript + Web TypeScript/Vite 构建 | 通过（单独执行 npm run build） |
| 本地 Chromium 场景 | 13 / 15 符合预期 |
| 导出的测试体 | 对 15 个相同 fixture 执行真实 Playwright 动作和断言；注册场景切换与执行器结论不一致 |
| UI 冒烟 | 默认空值、计划预览、确认门禁、修改期望使计划失效、提交载荷、重新编辑保留计划均通过 |
| 计划无前置条件校验 | 拒绝无场景前置断言的计划，通过 |

浏览器测试只访问临时本地 HTTP fixture，使用合成输入。没有调用 DeepSeek、发送短信、创建真实账号或发布 GitHub 报告。测试结束已关闭浏览器和测试服务，删除仅本轮生成的临时截图；仓库原有运行记录与截图未删除。

### 场景明细

| 场景 | 期望执行器结论 | 实际 | 导出测试体 | 验收 |
| --- | --- | --- | --- | --- |
| 正常输入 | 此路径未复现 | 一致 | 通过 | 通过 |
| 只读输入 | 已复现 | 一致 | 失败，符合故障预期 | 通过 |
| 禁用输入 | 已复现 | 一致 | 失败，符合故障预期 | 通过 |
| 失焦后清空 | 已复现 | 一致 | 失败，符合故障预期 | 通过 |
| 未进入注册场景 | 证据不足 | 一致 | 失败 | 通过 |
| 多个同名控件 | 证据不足 | 一致 | 失败 | 通过 |
| 无关 Console 错误及 404 | 此路径未复现 | 一致 | 通过 | 通过 |
| 购物车数量不更新 | 已复现 | 一致 | 失败，符合故障预期 | 通过 |
| 元素按预期隐藏 | 此路径未复现 | 一致 | 通过 | 通过 |
| 期望提示未出现 | 已复现 | 证据不足 | 失败 | 未通过 |
| 保存后刷新丢失 | 已复现 | 一致 | 失败，符合故障预期 | 通过 |
| 同源请求状态与 count 字段 | 此路径未复现 | 一致 | 通过 | 通过 |
| 未授权表单提交 | 证据不足 | 一致 | 失败 | 通过 |
| 不支持的富文本输入 | 证据不足 | 一致 | 失败 | 通过 |
| 点击注册入口后入口移除 | 此路径未复现 | 证据不足 | 通过 | 未通过 |

### 问题 1：动作成功后旧目标消失，证据采集反而终止流程（高优先级）

位置：apps/api/src/business.ts，executePlan 的动作后 observe(page, target)，当前约第 172 行。

复现：登录页点击 Register tab；注册表单显示，同时入口按钮移除。点击本身成功，但随后 observe 仍读取旧按钮，locator.evaluate 等待 4 秒超时，被同一个 catch 标为 blocked，注册表单输入步骤全部 skipped。

单独重复运行 switch 场景可稳定复现。导出测试没有这一步旧目标采集，因此可以完成注册表单输入；两条执行路径结论不一致。

建议：将动作后的辅助采集失败与动作/断言失败分开。入口因导航、DOM 更新消失时，采集当前页面状态或记录“原目标已消失”，不自动将成功动作降级为受阻。真正的下一步场景断言仍应独立执行。

### 问题 2：核心 visible 断言被通用定位前置检查截断（中优先级）

位置：apps/api/src/business.ts，第 98–99 行的 toHaveCount / toBeVisible，早于核心 assertion 分支。

复现：已确认目标场景，核心要求出现 Saved 提示，但页面未出现该提示。程序在进入核心 visible 检查之前就以定位失败返回 blocked，最终为 inconclusive，无法表达“指定提示未出现”。

建议：区分“操作目标找不到”和“核心断言要求目标出现但没有出现”。对用户确认过的 visible 检查，零匹配属于待验证的业务状态；多匹配仍应按歧义处理。适当保留场景前置校验，避免把未进入页面误判成 Bug。

### 问题 3：旧 GitHub 报告测试预期未同步（低优先级）

位置：apps/api/tests/github-publisher.test.ts:43。

测试使用没有 business 字段的旧记录，仍断言包含“Playwright 回归测试：已生成”。v0.5 实际显示“草稿 / 尚未验证”，符合新版对旧记录的保守处理。应更新旧记录预期，并另补有完整业务证据的 generated / draft 场景，而不是回退产品文案凑通过。

### 重跑方式

在项目根目录执行：

~~~sh
npm run check
npm run build
node scripts/verify-v05.mjs
node scripts/verify-v05-ui.mjs
~~~

定位单场景：

~~~sh
node scripts/verify-v05.mjs switch
node scripts/verify-v05.mjs missingMessage
~~~

前两个已知缺陷未修复前，浏览器验证脚本返回非零退出码是预期结果，不应删除断言来规避失败。

### 尚未覆盖

- 真实 DeepSeek 规划质量与模型故障降级。
- API 创建/存储/SSE 的完整端到端链路，UI 使用拦截响应。
- GitHub 真实 Check / 评论发布。
- 全设备矩阵、iframe、真实网站及复杂异步竞态。
- 导出文件在完整 Playwright runner / CI 环境下的类型检查与运行；本轮对导出测试体做了 TS 转换后实际执行，不等同于完整 runner 验证。
- 输入后长期延迟回滚、超时压力和全部敏感数据泄漏边界。

本次结果说明“输入能力验证”主线能够检出常见故障，但注册页切换是优先修复项。修复后应重跑上述两个失败场景及现有用例，再考虑完整发布验收。
## 评测实验室增量验证（2026-09-12）

本节为最新增量验证；上方历史报告的未覆盖项不代表本轮结果。

- 单元测试：8 个测试文件、29 条测试通过；前后端 TypeScript 与 Vite 构建通过。
- 真实浏览器冒烟：`blur-bug`、`blur-fixed`、`ambiguous-bug`、`danger-bug` 共 4 条均符合预期；删除动作未执行。
- 生成回归测试：同一文件通过完整 Playwright runner 分别执行，缺陷版在失焦后值断言处失败，修复版通过。收紧后的判分同时检查报错行、期望值、实际值和进程退出码。
- API / UI：非法 suite 返回 422，重复启动返回 409，未知演示场景返回 404；页面启动、轮询、JSON 下载、步骤证据展开及双版本页面交互通过。
- 视口：1440×1000、390×844；无横向溢出，浏览器未捕获页面脚本异常。
- 产物 ID：`be1cd27f-23b2-4ade-8e24-0cea3c8def96`，对应 `artifacts/eval-<id>/report.json`。

本机 Chromium 下载停滞，使用 `REPROLENS_EVAL_CHANNEL=chrome` 的隔离无头 Chrome 完成浏览器验证，未访问个人浏览器资料。环境信息见产物 manifest。

范围限制：仅实际运行 4 条冒烟用例及双版本生成测试；其余 16 条已验证 Schema 和成对计划一致性，未执行完整浏览器矩阵。没有真实模型调用，不把这次 4/4 成绩作为模型准确率或真实网站成功率。

---
## 回归 UI 与交互复现主线（2026-09-12）

- 一级导航恢复为工作台与运行记录，评测移入折叠的开发者工具；工作台载入示例仍必须确认计划才能执行。
- 新增 `unobscured` 检查，执行器与导出测试共用浏览器函数。单元测试覆盖中心遮挡、子元素命中、出视口及零面积、Schema 和代码生成；合计 9 个文件、33 条测试通过，前后端构建通过。
- `verify-reproduction.mjs` 经真实 API 创建任务，不拦截或伪造任务结果。UI 缺陷版：desktop 未复现、iphone13 已复现，失败证据记录中心命中工具栏；修复版两设备均未复现，计划完全一致。
- 输入失焦丢值示例：缺陷版已复现，修复版未复现。与 UI 示例都使用主任务执行器，不调用模型规划。
- UI 生成回归测试通过完整 Playwright runner 重跑同一文件：缺陷版桌面通过、手机在中心命中断言处失败；修复版两项通过。
- 首轮完整通过的任务：`aaeb51a4-2cad-4cb9-ab3d-d6cf7095bb11`、`b4b712d7-ad8a-40ed-a61e-d399ac035f77`、`eefa7700-1724-4b34-aa48-f74dfb65ec7a`、`5543857f-b1c7-45ab-936b-51d989a3d231`。产物位于 `artifacts/reproduction-qa-1789148160539/`。
- 使用隔离 Chrome；未覆盖缩放、iframe、多点完整遮挡检测、设计稿一致性与真实网站大样本。中心命中不是视觉正确性的通用证明。
- 最终界面复核：1440×1000 桌面与 390×844 手机视口通过，示例按钮文字可见、最近运行列表不再随表单拉伸、开发者工具可打开，无横向溢出及页面脚本异常。再次完成真实 API 双版本任务和导出测试重放，全部符合预期；截图与报告位于 `artifacts/reproduction-qa-1789172414037/`。

## 页面观察与小规模模型对照

- `npm run check`：10 个测试文件、37 条测试通过，前后端构建通过。
- `verify-observation.mjs`：真实 Chrome 验证候选元数据、表单值和密码排除、POST/跨域/HTTP 重定向阻止。初版发现浏览器路由不会重新检查所有重定向，改为无重定向抓取后复核通过。
- 1440×1000 与 390×844 工作台通过，无横向溢出或脚本异常；观察授权默认关闭。截图位于 `artifacts/observation-ui/`。
- 真实 `/api/plans`：未授权 origin 返回 422；本地 `/demo/profile` 观察后模型返回 200、4 个元素的观察提示与两步待确认计划。此次集成验证调用不计入对照样本。
- 模型对照第一轮两组 0/3，暴露必填字段遗漏和步骤契约理解错误；保留原始失败，不放宽 Schema。澄清 Prompt 后第二轮 baseline 0/3、observed 3/3，分别平均 6.35s / 13.12s（包括失败提前结束与双版本执行，不是单纯模型响应时延）。
- 两组第二轮统一使用 `observe-plan-2`；模板降级、错误目标、错误核心检查及执行异常全部计为失败。每份计划在内置缺陷/修复双版本原样执行。原始结果与适用范围见 [模型对照说明](#model-evaluation)。
- 仅 3 个合成输入场景、桌面、每组单次，且样本用于开发调试；没有留出集结论、统计显著性或真实网站泛化保证。未实现自主纠错、生产网络隔离及多租户服务。

## 默认聚焦 Bug 复现（2026-09-14）

- 新建任务的 `qualityScan` 默认 false：跳过性能观察器、通用 DOM 审计和 axe，不生成质量报告、评分或门禁。显式 true 仍生成附加报告；Console / HTTP 错误保留为辅助证据。
- 结果页优先呈现问题、预期、验证范围及失败/受阻步骤；其他步骤和像素对比折叠。未扫描不显示 A11y / 性能零值，证据不足与执行失败不再显示成功图标。
- `npm run check`：10 个文件、37 条单元测试通过，前后端构建通过；`git diff --check` 通过。
- `scripts/verify-reproduction.mjs`：7 个真实本机任务通过，覆盖 UI 缺陷/修复、输入缺陷/修复、显式质量扫描、关闭扫描仍保留 Console / HTTP 错误，以及前置条件失败判为 inconclusive。
- 同一份生成测试通过完整 Playwright runner 验证：缺陷版桌面通过、手机在遮挡断言处失败；修复版均通过。1440×1000 / 390×844 无横向溢出或脚本异常，手机重试按钮文字可见。
- 最终截图与报告：`artifacts/reproduction-qa-1789396355022/`。另通过真实 UI 打开旧任务 `ec93e4a7-d60d-4f8a-9f25-486d9e93b493`，确认没有 `qualityScan` 字段时仍可展开已有质量报告。
- 本轮不调用模型，不运行大规模网站矩阵；修复重放仍以核心步骤为准，没有新增自动根因定位或通用修复证明。

## 业务修复验证闭环（2026-09-15）

- 新增 `verification.business`：核对确认计划、设备、问题和预期，按设备与步骤索引关联证据。修复通过要求基线确有核心失败且本次所有步骤通过；不信任缓存的 covered/total 代替逐项证据。
- 54 条单元测试通过，包含计划/设备/预期变更、执行失败、证据缺失/重复、跳过、其他核心失败、基线未复现、旧记录，以及质量评分不能覆盖业务失败。前后端 TypeScript 与 Vite 构建通过。
- 真实 Chrome 回归共 11 个任务：保留原有 UI/输入双版本与质量开关测试，新增修复通过、原问题仍复现、错误页面前置条件受阻、断言改变不可比较，以及页面加载失败仍持久化 inconclusive。
- 同一导出文件通过完整 Playwright runner：缺陷版桌面通过、手机在遮挡断言失败；修复版均通过。桌面 1440×1000、手机 390×844 检查无横向溢出或页面脚本异常。
- 结果页展示原失败项通过数量、其他失败数量、逐项前后状态/期望/实际与截图链接。截图差异生成失败仅记录警告，不覆盖业务结论；GitHub Check 对不可比较和证据不足返回 neutral。
- 最终浏览器报告与截图：`artifacts/reproduction-qa-1789484942090/`。失败加载任务为 `90bec962-0529-4916-8a12-719b4b36168b`，修复通过任务为 `0c4a04a0-5bcc-421c-a285-c13b8fa5ce39`。
- 本轮未调用真实模型、未向 GitHub 发布 Check，也未进行生产站点大规模验证；仅证明已确认计划与指定设备范围内的结果，不作自动根因判断。

</details>
